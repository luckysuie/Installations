# Python installation and verification
------------------

sudo apt update
sudo apt install python3 python3-pip python3-venv pytest -y
python3 --version
pip3 --version
venv --version
pytest --version
